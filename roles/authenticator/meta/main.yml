# SPDX-FileCopyrightText: Copyright © 2026, Jared Cook
# SPDX-License-Identifier: AGPL-3.0-or-later
---
galaxy_info:
  author: "Jared Cook"
  description: "Install Authenticator flatpak."
  # company: "Optional Company Name"

  # Must use valid SPDX identifiers like 'GPL-3.0-or-later'
  license: "AGPL-3.0-or-later"

  min_ansible_version: "2.10"
  role_name: authenticator

  # Galaxy/Automation Hub uses these to categorize your role
  platforms:
    - name: EL
      versions:
        - "8"
        - "9"
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - flatpak
    - desktop
    - apps

dependencies: []
